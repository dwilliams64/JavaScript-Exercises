// Chanllenge

// Create an object with these three properties:
// name, age, location

// Have the following message print
// Dimitri is 35 and lives in San Antonio

// Increase age by 1 and print message again

let person = {
    name: 'Dimitri',
    age: 35,
    location: 'San Antonio'
}

console.log(`${person.name} is ${person.age} and lives in ${person.location}`);

person.age += 1;

console.log(`${person.name} is ${person.age} and lives in ${person.location}`);